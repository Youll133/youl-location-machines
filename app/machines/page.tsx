import Machines from "../../components/Machines";

export default function MachinesPage() {
  return (
    <main
      style={{
        paddingTop: "100px",
        background: "#f3f4f6",
        minHeight: "100vh",
      }}
    >
      <Machines />
    </main>
  );
}